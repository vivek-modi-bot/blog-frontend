# Blog Frontend

React + Vite frontend for the blog site. Uses Google OAuth via the FastAPI backend.

## Setup

```bash
npm install
npm run dev
```

App runs at http://localhost:5173

Set `VITE_API_URL` if the API is not at `http://localhost:8000`.
